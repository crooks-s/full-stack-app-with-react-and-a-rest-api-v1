import { useState } from "react";

/*
Render COURSES screen -- 
  1. get list of courses from API's '/api/courses' route then render the list
  2. each course needs a LINK to its 'Course Detail' screen
  3. render a link to 'Create Course' screen
*/

const Courses = () => {

}

export default Courses;
